<Window x:Class="TodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoApp"
        mc:Ignorable="d"
        Title="Список задач" Height="350" Width="400">
    <StackPanel Margin="10">
        <TextBox x:Name="NewTaskTextBox" Margin="0,0,0,10"/>
        <Button Content="Добавить" Click="AddTask_Click"/>

        <ListView x:Name="TasksListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}" 
                              Margin="0,0,10,0"
                              VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Content="Удалить выбранное" 
            Click="DeleteTask_Click" 
            Margin="0,10,0,0"/>
    </StackPanel>
</Window>
